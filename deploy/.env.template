# ─── 지자체 정보 (★ 납품 시 변경) ───
ORG_NAME=제주시
DOMAIN=parkmaster.jeju.go.kr

# ─── 보안 키 (자동 생성) ───
POSTGRES_PASSWORD=생성필요_32자_랜덤
JWT_SECRET=생성필요_64자_랜덤
ANON_KEY=생성필요
SERVICE_ROLE_KEY=생성필요

# ─── URL ───
SITE_URL=https://${DOMAIN}
API_EXTERNAL_URL=https://${DOMAIN}

# ─── 포트 ───
POSTGRES_PORT=5432
KONG_HTTP_PORT=8000

# ─── 백업 ───
BACKUP_SCHEDULE=0 2 * * *
BACKUP_RETENTION_DAYS=90

# ─── 카카오맵 ───
KAKAO_MAP_KEY=발급필요

# ─── AI (선택) ───
AI_ENABLED=false

# ─── 메시지 (선택) ───
ALIMTALK_ENABLED=false
SMS_ENABLED=false
